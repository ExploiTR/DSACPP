#include "../../header.h"
using namespace std;

/*
 * Given an integer array nums, return 1
 * if there exists a triplet [nums[i], nums[j], nums[k]]
 * such that i != j, i != k, and j != k,
 * and nums[i] + nums[j] + nums[k] == 0,
 * else return 0.
 */

bool threeSum(int *nums, int size)
{
    
    return false;
}

int main()
{
    iosetup();

    return 0;
}