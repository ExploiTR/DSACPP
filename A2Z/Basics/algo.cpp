#include "../header.h"

/*
 * Euclidean Algorithm : gdc(a,b) = gcd(a-b,b) : a > b
                                = gcd(a%b,b) ...... IF A==B -> gcd = b VICE VERSA
 */
int main()
{
    iosetup();

    // comparator

    // if the conditon is what u want return true or return false for a swap

    // int cnt = __builtin_popcount(7);
    // long long num = 7347843827348724353;
    // int cnt2 = __builtin_popcountll(num);

    // cout << cnt << endl
    //      << cnt2 << endl;

    // max_element();
    // min_element
    // next_permuation()

    return 0;
}